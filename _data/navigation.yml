header:
  - titles:
      # @start locale config
      en      : &EN       Research
      # @end locale config
    url: /research

  - titles:
      # @start locale config
      en      : &EN       Publications
      # @end locale config
    url: /publications

  - titles:
      # @start locale config
      en      : &EN       Projects
      # @end locale config
    url: /projects

  - titles:
      # @start locale config
      en      : &EN       Members
      # @end locale config
    url: /members

  - titles:
      # @start locale config
      en      : &EN       News
      # @end locale config
    url: /news
